<nav class="px-2 max-w-3xl my-4 mx-auto">
    <ul class="flex">
        <li class="border mr-2 flex-1 text-center hover:bg-gray-600 hover:text-white" :class="selectedSport == 'swim' && 'bg-gray-600 text-white'">
            <button type="button" @click="selectedSport = 'swim'" class="block w-full p-2">Swim</button>
        </li>
        <li  class="border mr-2 flex-1 text-center hover:bg-gray-600 hover:text-white" :class="selectedSport == 'bike' && 'bg-gray-600 text-white'">
            <button type="button" @click="selectedSport = 'bike'" class="block w-full p-2">Bike</button>
        </li>
        <li class="border flex-1 text-center hover:bg-gray-600 hover:text-white" :class="selectedSport == 'run' && 'bg-gray-600 text-white'">
            <button type="button" @click="selectedSport = 'run'" class="block w-full p-2">Run</button>
        </li>
    </ul>
</nav>